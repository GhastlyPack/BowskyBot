services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: bowskybot
      POSTGRES_PASSWORD: bowskybot
      POSTGRES_DB: bowskybot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
